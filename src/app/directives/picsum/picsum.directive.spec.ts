import { PicsumDirective } from './picsum.directive';

describe('PicsumDirective', () => {
  it('should create an instance', () => {
    const directive = new PicsumDirective();
    expect(directive).toBeTruthy();
  });
});
